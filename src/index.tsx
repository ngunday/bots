import * as React from 'react';
import * as ReactDOM from 'react-dom';

// @ts-ignore Type missing deep link param
fin.desktop.main(async (args) => {
    ReactDOM.render(<div> OpenFin Bots Stub </div>, document.getElementById('main'));
});
